<UserControl x:Class="Sweng500.Pml.Client.Views.MainWorkspaceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" 
             d:DesignWidth="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <DataGrid ItemsSource="{Binding TheCollection}" 
                  AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Title"
                                    Binding="{Binding Path=Title}" />
                <DataGridTextColumn Header="Publisher"
                                    Binding="{Binding Path=Publisher}" />
                <DataGridTextColumn Header="Description"
                                    Binding="{Binding Path=MediaDescription}" />
            </DataGrid.Columns>
        </DataGrid>
        
        <ListBox Grid.Column="1" 
                 ItemsSource="{Binding TheCollection}"
                 Margin="15"/>

        <ContentControl Grid.Row="1"
                        Grid.ColumnSpan="2"
                        Content="{Binding TempEditor}" Margin="81,37,-81,-37" />
    </Grid>
</UserControl>
